# en-US-specific allowlist (merged with the global config/dictionary.txt).
# Seeded from the stillwater calibration run. Comparison is case-insensitive,
# so one entry covers all casings. Domain/media terms and dev jargon that
# LanguageTool's dictionary does not know.

# Media / metadata domain
NFO
NFOs
Kodi
Emby
Jellyfin
Lidarr
fanart
Discogs
AudioDB
TheAudioDB
MusicBrainz
MBID
SortName
MusicArtist
ProviderIds
ReplaceAllImages
lockdata
writeback

# Dev / infra jargon
filesystem
worktree
worktrees
goroutine
goroutines
templ
OpenAPI
Traefik
ZeroSSL
EAB
toolchain
prereqs
heredoc
heredocs
substring
substrings
speedup
speedups
SHA
SHAs
rc

# CI runner labels. Like the browser engines below, the case-insensitive match
# is what covers the lowercase identifier (`ubuntu-latest`, `macos-latest`) as
# it appears verbatim in a workflow or a gate report.
ubuntu
macos

# Browser engines (proper nouns; case-insensitive match also covers the
# lowercase Playwright CLI identifiers firefox/webkit)
Firefox
WebKit

# Brand / design
wordmark
